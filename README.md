# Sagun Management System

Clean production project structure.

## Main folders
- `html/` — application pages
- `css/` — page styles
- `js/` — application JavaScript
- `assets/` — images, fonts and media
- `supabase/` — database SQL, config and Edge Functions

## Root files
- `index.html` — landing page
- `manifest.json` — PWA manifest
- `_headers` — hosting headers

## Cleanup performed
- Removed old backup files and unused duplicate page versions.
- Removed editor/project metadata and temporary Supabase linkage metadata.
- Removed documentation/test placeholders that are not required to run the app.
- Kept application HTML/CSS/JS, Supabase files and all referenced assets intact.
